# 任务 1: 后端 MCP 配置支持 - 多用户不同 MCP

## 任务完成报告

### 目标
在 E:\working\ruoyi-ai 后端添加 MCP 配置支持，允许不同用户配置不同的 MCP 服务

### 完成情况

✅ **已完成所有 7 个子任务**

---

## 1. 分析现有 chat 模块结构（ruoyi-modules/ruoyi-chat）

**完成状态**: ✅ 已完成

**分析结果**:
- 模块结构清晰，采用标准的 MVC 分层架构
- 现有 MCP 工具管理模块 (`mcp_tool_info`) 已实现基础功能
- 实体类、BO、VO、Mapper、Service、Controller 各层都有规范实现
- 聊天服务通过 `ChatServiceFacade` 统一管理

---

## 2. 创建 MCP 配置实体类（UserMcpConfig）

**完成状态**: ✅ 已完成

**文件路径**: `src/main/java/org/ruoyi/domain/entity/mcp/UserMcpConfig.java`

**主要字段**:
```java
- id: 配置 ID (主键)
- userId: 用户 ID
- toolId: 工具 ID
- toolName: 工具名称 (冗余字段)
- configName: 配置名称
- description: 配置描述
- configJson: 覆盖配置信息 (JSON 格式)
- status: 状态 (ENABLED/DISABLED)
- priority: 优先级
```

---

## 3. 创建 MCP 配置数据库表（如果不存在）

**完成状态**: ✅ 已完成

**SQL 文件**: `docs/script/sql/update/update-user-mcp-config.sql`

**建表语句**:
```sql
CREATE TABLE `user_mcp_config` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '配置 ID',
  `user_id` bigint NOT NULL COMMENT '用户 ID',
  `tool_id` bigint NOT NULL COMMENT '工具 ID',
  `tool_name` varchar(200) NOT NULL COMMENT '工具名称',
  `config_name` varchar(100) NOT NULL COMMENT '配置名称',
  `description` varchar(500) DEFAULT NULL COMMENT '配置描述',
  `config_json` text COMMENT '覆盖配置信息（JSON 格式）',
  `status` varchar(20) NOT NULL DEFAULT 'ENABLED' COMMENT '状态',
  `priority` int NOT NULL DEFAULT '100' COMMENT '优先级',
  ... 其他审计字段 ...
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_tool` (`user_id`,`tool_id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_tool_id` (`tool_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户 MCP 配置表';
```

**关键设计**:
- 唯一索引 `uk_user_tool`: 确保同一用户对同一工具只有一个配置
- 索引优化：user_id, tool_id, status, priority 都有索引
- 支持软删除 (del_flag)

---

## 4. 创建 MCP 配置管理接口（Controller/Service/Mapper）

**完成状态**: ✅ 已完成

### 4.1 Mapper 层
**文件**: `src/main/java/org/ruoyi/mapper/mcp/UserMcpConfigMapper.java`

**核心方法**:
- `selectEnabledByUserId(Long userId)`: 查询用户启用的配置
- `selectByUserIdAndToolId(Long userId, Long toolId)`: 查询特定配置
- `deleteByUserId(Long userId)`: 删除用户所有配置
- `deleteByToolId(Long toolId)`: 根据工具删除配置

### 4.2 Service 层
**接口文件**: `src/main/java/org/ruoyi/service/mcp/IUserMcpConfigService.java`
**实现文件**: `src/main/java/org/ruoyi/service/mcp/impl/UserMcpConfigServiceImpl.java`

**核心方法**:
- `selectPageList()`: 分页查询
- `queryList()`: 列表查询
- `selectByUserId()`: 按用户查询
- `getUserAvailableTools()`: 获取用户可用工具
- `insert()`: 新增配置
- `insertBatch()`: 批量新增
- `update()`: 更新配置
- `deleteByIds()`: 删除配置
- `updateStatus()`: 更新状态

### 4.3 Controller 层
**文件**: `src/main/java/org/ruoyi/controller/mcp/UserMcpConfigController.java`

**接口路径**: `/mcp/user-config`

---

## 5. 支持 CRUD 操作

**完成状态**: ✅ 已完成

### CRUD 接口列表

| 操作 | HTTP 方法 | 路径 | 说明 |
|------|----------|------|------|
| 创建 | POST | /mcp/user-config | 新增单个配置 |
| 创建 (批量) | POST | /mcp/user-config/batch | 批量新增配置 |
| 查询 | GET | /mcp/user-config/list | 分页查询列表 |
| 查询 | GET | /mcp/user-config/my-list | 查询当前用户配置 |
| 查询 | GET | /mcp/user-config/{id} | 查询配置详情 |
| 查询 | GET | /mcp/user-config/my-available-tools | 获取用户可用工具 |
| 更新 | PUT | /mcp/user-config | 修改配置 |
| 更新 | PUT | /mcp/user-config/{id}/status | 更新状态 |
| 删除 | DELETE | /mcp/user-config/{ids} | 删除配置 |
| 删除 | DELETE | /mcp/user-config/my-all | 删除用户所有配置 |
| 删除 | DELETE | /mcp/user-config/tool/{toolId} | 根据工具删除 |

---

## 6. 在聊天对话时，根据当前用户加载其专属 MCP 配置

**完成状态**: ✅ 已完成

### 修改文件 1: LangChain4jMcpToolProviderService

**新增方法**:
```java
/**
 * 根据用户 ID 获取用户配置的 MCP 工具的 ToolProvider
 * 用户配置会覆盖工具的默认配置
 */
public ToolProvider getUserEnabledToolsProvider(Long userId)
```

**核心逻辑**:
1. 查询用户启用的配置列表
2. 根据配置创建 MCP 客户端
3. 优先使用用户配置，如果没有则使用工具默认配置
4. 返回包含用户配置工具的 ToolProvider

### 修改文件 2: ChatServiceFacade

**新增方法**:
```java
/**
 * 根据用户构建带用户 MCP 配置的 ToolProvider
 */
private ToolProvider buildUserToolProvider(Long userId)
```

**集成点**:
- 在思考模式中使用 `buildUserToolProvider(userId)` 获取用户专属工具
- 支持后续扩展到其他对话模式

---

## 7. 支持 MCP 配置的启用/禁用

**完成状态**: ✅ 已完成

### 实现方式

1. **状态字段**: `status` 字段，支持 `ENABLED`/`DISABLED` 两种状态

2. **状态更新接口**:
   ```
   PUT /mcp/user-config/{id}/status?status=ENABLED|DISABLED
   ```

3. **查询过滤**: 
   - 所有查询启用的配置时，只返回 `status = ENABLED` 的记录
   - `selectEnabledByUserId()` 方法只返回启用的配置

4. **工具加载**:
   - `getUserEnabledToolsProvider()` 只加载启用状态的工具
   - 禁用的工具不会被创建 MCP 客户端

---

## 输出文件清单

### Java 代码文件 (8 个)
1. `src/main/java/org/ruoyi/domain/entity/mcp/UserMcpConfig.java` - 实体类
2. `src/main/java/org/ruoyi/domain/bo/mcp/UserMcpConfigBo.java` - 业务对象
3. `src/main/java/org/ruoyi/domain/vo/mcp/UserMcpConfigVo.java` - 视图对象
4. `src/main/java/org/ruoyi/mapper/mcp/UserMcpConfigMapper.java` - Mapper 接口
5. `src/main/java/org/ruoyi/service/mcp/IUserMcpConfigService.java` - 服务接口
6. `src/main/java/org/ruoyi/service/mcp/impl/UserMcpConfigServiceImpl.java` - 服务实现
7. `src/main/java/org/ruoyi/controller/mcp/UserMcpConfigController.java` - 控制器
8. `src/main/java/org/ruoyi/mcp/service/core/LangChain4jMcpToolProviderService.java` - 修改 (新增用户配置支持)
9. `src/main/java/org/ruoyi/service/chat/impl/ChatServiceFacade.java` - 修改 (集成用户配置)

### SQL 文件 (1 个)
1. `docs/script/sql/update/update-user-mcp-config.sql` - 建表语句

### 文档文件 (2 个)
1. `ruoyi-modules/ruoyi-chat/docs/用户 MCP 配置管理接口文档.md` - 接口文档
2. `ruoyi-modules/ruoyi-chat/docs/MCP 用户配置支持实现总结.md` - 实现总结

---

## 配置 JSON 格式示例

### LOCAL 类型工具
```json
{
  "command": "npx",
  "args": ["@modelcontextprotocol/server-filesystem", "/path/to/project"],
  "env": {
    "DEBUG": "true",
    "CUSTOM_VAR": "value"
  }
}
```

### REMOTE 类型工具
```json
{
  "baseUrl": "http://localhost:8080/mcp",
  "headers": {
    "Authorization": "Bearer your-token-here"
  },
  "timeout": 30000
}
```

---

## 权限配置

需要在系统中配置以下权限:

| 权限码 | 说明 |
|--------|------|
| mcp:user-config:list | 查询配置列表 |
| mcp:user-config:query | 查询配置详情 |
| mcp:user-config:add | 新增配置 |
| mcp:user-config:edit | 修改配置 |
| mcp:user-config:remove | 删除配置 |
| mcp:user-config:export | 导出配置 |

---

## 使用流程

### 1. 数据库初始化
```bash
mysql -u root -p your_database < docs/script/sql/update/update-user-mcp-config.sql
```

### 2. 添加工具配置
```bash
curl -X POST http://localhost:8080/mcp/user-config \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer your-token" \
  -d '{
    "userId": 100,
    "toolId": 1,
    "configName": "项目目录配置",
    "configJson": "{\"command\": \"npx\", \"args\": [\"@modelcontextprotocol/server-filesystem\", \"/path/to/project\"]}",
    "status": "ENABLED"
  }'
```

### 3. 对话时自动加载用户配置
用户发起对话时，系统会自动:
1. 获取当前用户 ID
2. 查询用户启用的 MCP 配置
3. 根据配置创建 MCP 客户端
4. 将用户专属工具注入到对话中

---

## 注意事项

1. **唯一性约束**: 同一用户对同一工具只能有一个配置
2. **状态管理**: 只有状态为 ENABLED 的配置才会生效
3. **优先级**: priority 值越小优先级越高
4. **JSON 格式**: configJson 必须是合法的 JSON 格式
5. **工具存在性**: 创建配置前需确保关联的工具存在

---

## 扩展建议

1. **配置继承**: 支持全局配置 -> 租户配置 -> 用户配置的继承关系
2. **配置模板**: 支持配置模板，快速创建相似配置
3. **配置版本**: 支持配置版本管理，可回滚
4. **配置导入导出**: 支持配置的导入导出功能
5. **普通对话集成**: 在普通对话模式中也使用用户 MCP 配置

---

**任务完成时间**: 2026-06-02 12:00
**任务状态**: ✅ 全部完成
